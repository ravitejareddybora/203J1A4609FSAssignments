var reverseArray = function (array) {
    return array.reverse();
};
// Test
var originalArray = ['hi', 'hello'];
var reversedArray = reverseArray(originalArray);
console.log(reversedArray); // [5, 4, 3, 2, 1]
